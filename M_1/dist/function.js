"use strict";
{
    function add(x, y) {
        return (x + y);
    }
    ;
    const multiply = (x, y) => {
        return (x + y);
    };
    let wallet = {
        first_name: "Hello",
        balance: 0,
        addBalance(x) {
            return this.balance += x;
        }
    };
    const arr = [2, 3, 4, 5, 6];
    const new_arr = arr.map((el) => el * el);
}
